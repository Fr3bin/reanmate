# Copy to .env.local for local overrides. Leave empty to keep the mock demo.

# Node.js + MongoDB API from the backend teammate (no trailing slash).
# Example: http://localhost:4000/api
NEXT_PUBLIC_API_URL=
