# Server-only backend URL for auth proxying and session checks
API_URL=http://localhost:4000

# Optional public API base for remote AI features (no trailing slash).
# Leave empty to keep those features in mock mode.
NEXT_PUBLIC_API_URL=
